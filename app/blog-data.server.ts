// Auto-generated by api/ssg.rs — do not edit
export const posts = [{"slug":"ssg","title":"Building a SSG in Rust for my personal blog","date":"2025-10-15"},{"slug":"ai-chat-portfolio","title":"Adding a AI chat to interact with my personal site and some thoughts about AI","date":"2026-03-27"},{"slug":"mandarin-vocab","title":"普通话 — Building a Mandarin Vocabulary Catalog for My Daughter","date":"2026-04-14"}];
export const postsBySlug: Record<string, { slug: string; title: string; body: string; date: string }> = {"ssg":{"slug":"ssg","title":"Building a SSG in Rust for my personal blog","body":"<h1>Building a SSG in Rust for my personal blog</h1>\n<p>My intention in developing a static site generator to build my personal blog is to document my way to learn Rust and start once and for all my writing journey.</p>\n<h2>Static Site Generator</h2>\n<p>A SSG’s are system engines that use text input files to generate static pages.</p>\n<h2>Development</h2>\n<h3>General idea</h3>\n<ul>\n<li>Input markdown to the engine</li>\n<li>Having predefined templates to match that input</li>\n<li>Output the HTML result</li>\n<li>Generate a GraphQL endpoint to serve the generated pages/posts to a client</li>\n<li>Consume the GraphQL endpoints into a Remix client (my personal web page)</li>\n<li>Render the list of posts and the post details</li>\n</ul>\n<p>I decided to use <a href=\"https://docs.rs/tera/latest/tera/\">Tera</a> as template engine. I have a base template and a post template which extends from base and builds the post from a content folder which contains md files. I also added a GraphQL handler using <a href=\"https://docs.rs/async-graphql/latest/async_graphql/\">async_graphql</a> so in this way I can consume them and use them in my Remix portfolio as the client.</p>\n<h2>Technologies</h2>\n<ul>\n<li><strong>Rust</strong> — systems language used to build the SSG binary</li>\n<li><strong>pulldown-cmark</strong> — markdown to HTML parser</li>\n<li><strong>async-graphql</strong> — GraphQL server library (original architecture)</li>\n<li><strong>Axum</strong> — HTTP framework (original architecture)</li>\n<li><strong>Tera</strong> — template engine</li>\n<li><strong>Remix</strong> — React framework used as the client</li>\n<li><strong>Tailwind CSS</strong> — styling, with <code>@tailwindcss/typography</code> for blog post rendering</li>\n<li><strong>Vercel</strong> — deployment platform for the Remix frontend</li>\n<li><strong>Claude Code</strong> — AI coding assistant used throughout development</li>\n</ul>\n<h2>Deployment: What Nobody Tells You</h2>\n<p>When I set out to deploy this to Vercel, I assumed it would be a matter of a few config lines and a <code>vercel deploy</code>. It was not.</p>\n<h3>The Original Plan</h3>\n<p>The SSG started as an Axum web server. The Rust binary would run as a live GraphQL API, the Remix frontend would call it at request time to fetch posts. This required a hosted server running 24/7 — for a personal portfolio that felt like unnecessary overhead, so the next step was to move it to Vercel as a serverless function using <code>vercel_runtime</code>. No server to manage, pay for, or keep alive.</p>\n<p>This worked locally. Then came deployment.</p>\n<h3>Problem 1: <code>vercel-rust</code> is Abandoned</h3>\n<p>The first error was blunt:</p>\n<pre><code>Error: The following Serverless Functions contain an invalid &quot;runtime&quot;:\n  - api/graphql (provided)\n</code></pre>\n<p>The <code>vercel-rust</code> community runtime — the npm package responsible for compiling <code>.rs</code> files into Vercel serverless functions — was last seriously maintained in 2021. It depends on <code>@vercel/build-utils@2.x</code> while Vercel’s current platform runs <code>5.x</code>. The API changed, the package didn’t follow, and there is no official replacement. No amount of version tweaking fixes this.</p>\n<h3>Problem 2: Shuttle Wouldn’t Connect</h3>\n<p>The next idea was to host the Rust API on Shuttle.rs — a Rust-native cloud platform with a free tier. The code changes would be minimal: swap <code>vercel_runtime</code> for Shuttle’s handler macro and deploy.</p>\n<pre><code>cargo shuttle login\nError: failed to connect to auth endpoint\nOperation timed out (os error 60)\n</code></pre>\n<p>The CLI couldn’t connect to Shuttle’s auth endpoint at all. Another dead end.</p>\n<h3>Rethinking the Architecture</h3>\n<p>After two failed paths, the question became: does this API actually need to run live?</p>\n<p>The Rust code reads markdown files and converts them to HTML. That work doesn’t need to happen at request time — it can happen at deploy time. The result is the same HTML either way. This is what a static site generator is supposed to do.</p>\n<h3>The Fix: Build-time Generation</h3>\n<p>Instead of running as a live HTTP server, the Rust binary now runs once during the Vercel build:</p>\n<pre><code>Markdown files → Rust runs at deploy time → TypeScript module → Remix bundles it\n</code></pre>\n<p>The binary generates <code>app/blog-data.server.ts</code> containing all post data, which Remix bundles at build time. No live server. No external API. No runtime filesystem reads. The core Rust logic is unchanged — <code>find_content()</code>, <code>load_post()</code>, <code>pulldown-cmark</code> parsing — all the same. What changed is only the output: instead of an HTTP response, it writes a file to disk.</p>\n<h2>Working with Claude Code</h2>\n<p>This project was developed with Claude Code as an AI coding assistant. The experience taught me as much about working with AI tools as it did about Rust and deployment.</p>\n<h3>The Cost of Missing Context</h3>\n<p>Early in the deployment debugging, Claude suggested replacing the Rust blog API with a Node.js implementation that read the markdown files directly in Remix. The suggestion was technically sound. It would have worked. But the Rust code was the point — removing it would have defeated the purpose of the project entirely.</p>\n<p>The AI had no way of knowing that without being told. And I hadn’t told it.</p>\n<p>After pushing back, I asked directly: how can I work with you better so this doesn’t happen again? The answer was a <code>CLAUDE.md</code> file — a plain text file at the project root where you document architectural constraints, decisions, and things that must not change. Claude reads it at the start of every conversation.</p>\n<p>Adding a single constraint:</p>\n<pre><code>The Rust GraphQL API must remain functional, not just decorative. Do not replace Rust logic with Node.js equivalents.\n</code></pre>\n<p>Would have changed the entire direction of that conversation from the start.</p>\n<h3>AI Still Requires a Developer</h3>\n<p>Even with context, AI suggestions need to be validated. During this project, Claude proposed solutions that seemed reasonable but turned out to be wrong — packages that were abandoned, platform features that no longer existed, version numbers that didn’t exist on npm.</p>\n<p>The AI doesn’t know what it doesn’t know. It can’t check whether a library is actively maintained, whether a CLI tool actually connects, or whether a platform has changed its infrastructure since its training data was collected. That verification is still the developer’s job.</p>\n<p>The practical lesson: use AI to move fast, but own the decisions. Understand what every suggested change does before applying it. When something fails, read the error yourself rather than just passing it back and asking for a fix.</p>\n<h2>Lessons</h2>\n<p><strong>Check if your dependencies are maintained before building on them.</strong> <code>vercel-rust</code> had no commits in years. That’s a sign.</p>\n<p><strong>“Free tier” doesn’t mean “works.”</strong> Several platforms offer free Rust hosting in theory. In practice, CLI tools time out, runtimes are deprecated, and documentation lags behind reality.</p>\n<p><strong>Sometimes the architecture needs to change, not the config.</strong> The live GraphQL API wasn’t wrong — it just wasn’t necessary. Removing the requirement for a persistent server eliminated the entire class of hosting problems.</p>\n<p><strong>Give your AI assistant context upfront.</strong> A <code>CLAUDE.md</code> file with key architectural decisions saves significant back-and-forth and prevents suggestions that technically work but miss the point entirely.</p>\n<p><strong>AI is a tool, not a decision maker.</strong> The best use of Claude Code in this project was generating code quickly and exploring options. The decisions — what to keep, what to change, what mattered — those were mine.</p>\n<p>The Rust code is still in the repo. It still compiles. It still does exactly what it was written to do. It just doesn’t need to run 24/7 to do it.</p>\n","date":"2025-10-15"},"ai-chat-portfolio":{"slug":"ai-chat-portfolio","title":"Adding a AI chat to interact with my personal site and some thoughts about AI","body":"<h1>Adding a AI chat to interact with my personal site and some thoughts about AI</h1>\n<h2>Why?</h2>\n<h3>Interactive portfolio</h3>\n<p>I always wanted an interactive portfolio where the visitors really have fun and feel emotions. Thats why the idea to have a chat where you can ask and being answered came natural to this concept.</p>\n<h3>AI is taking our jobs</h3>\n<p>As many of my peers currently Im looking for a job, in a context where AI is “taking our jobs”\nBuilding this feature into my personal space is in part a way to explore the capabilities of this technology but there’s also, I have to admit, a sarcastic aspect in it, the human in me reclaiming her place in the collective HUMAN creativity.\nAre “AI” “agents” “LLMs” “prompt“ “assisted“ the new buzz words? So let’s build something with what they want to find now in our resume ;)</p>\n<h3>The IndieWeb</h3>\n<p>I currently found out about the <a href=\"https://indieweb.org/\">IndieWeb</a>   community and I got very excited because to own my “digital corner” and make it mine was the first motivation to start building my site. </p>\n<p>From the moment I conceveid to this site I wanted it to be a digital playground for my creativity regarding web development as many of my artistic expressions.</p>\n<p>I think that cultivate intelligence (the real one) is crucial in this era, Im still learning about The IndieWeb but I feel that if you’re a rebel like me and like many others you should check it out and start building and owning your digital space, let’s make the internet fun again!</p>\n<h2>The Idea</h2>\n<p>My portfolio is very simple. It consists of a main card which flips. In the first version when the user flips the card you can read a text showing more about my tech stack, this was ok for a first version but I wanted something more interactive.\nSo taking advantage of the card effect I decided that the AI chat must replace the “About” text on the back part of the Card instead.\nI wanted a simple chat window which can answer questions about my career, my experience, my state of currently looking for a job and very important to highlight the work done in this personal site.</p>\n<h2>The Architecture</h2>\n<p>The stack was already Remix + Vercel, so I leaned into what the framework gives you.</p>\n<p><strong>Server-side action, not a client-side fetch to OpenAI directly.</strong> I could have called the OpenAI API from the browser, but that would expose the API key. Remix’s <code>action</code> functions run on the server, so the key never leaves the backend. The frontend just POSTs to <code>/api/chat</code> and gets a reply back.</p>\n<p><strong>gpt-4o-mini over gpt-4o.</strong> For this use case Mini is around 15x cheaper per token. </p>\n<p><strong>The system prompt as text</strong> </p>\n<p><strong>Conversation history in the client.</strong> Remix actions are stateless — there’s no session store between requests. Rather than adding a database or Redis just to track chat history, I keep the conversation array in React state and send it with each POST. The server forwards it to OpenAI alongside the new message. The model gets full context, no persistence layer required.</p>\n<h2>The UI Decisions</h2>\n<p><strong>Suggestion chips on empty state.</strong> The hardest UX problem with a chat interface is the blank slate — people freeze when they don’t know what to ask. Four chips handle this: “What’s your stack?”, “Tell me about Web3 work”, “Are you open to work?”, “What’s your best project?”. They also double as a soft signal of what the AI is good at answering.</p>\n<p><strong>Typing indicator.</strong> Three bouncing dots while the AI is responding. Small detail, but it makes the wait feel intentional rather than broken.</p>\n<p><strong>Flip back button.</strong> The card back has a <code>← back</code> button that flips to the front. Clicking the input or send button doesn’t accidentally re-flip the card.</p>\n<h2>Implementation</h2>\n<h3>UI related</h3>\n<h4>The chat:</h4>\n<p>At the back of the Card there is this CardChat component which consist on the below parts</p>\n<ul>\n<li><strong>Message list</strong>:  scrollable area that renders user and assistant bubbles differently. \nSuggested prompts: shown when the conversation is empty. Clicking one calls handleSend directly with the suggestion text, skipping the input field. This makes the first interaction frictionless:</li>\n</ul>\n<pre><code>const SUGGESTIONS = [\n    &quot;What's your tech stack?&quot;,\n    &quot;Tell me about Web3 work&quot;,\n    &quot;Are you open to work?&quot;,\n    &quot;What's your best project?&quot;,\n];\n</code></pre>\n<ul>\n<li><strong>Loading indicator</strong>:  three bouncing dots using animate-bounce with staggered animation-delay values. A small touch, but it makes the response feel alive while the API call is in flight.</li>\n</ul>\n<h3>The System Prompt and some thoughts about subscriptions</h3>\n<p>The system prompt is where the AI gets its “knowledge.” It’s a compact block of text with my experience, skills, availability, and some aspects of my personality. </p>\n<p>One of the constrains I found when aspiring to make the context of “my AI” to be very detailed,   I needed to keep it tight to minimize token usage and this limited the precision about “me” that I really wanted to give to this project.\nAt the beggining I added my full resume as a JavaScript object, and wanted the context to contemplate my blog post contents, know about my life outside work, my hobbies and really make it “know a lot about me” so people can relate.\nBut you know everything is a payed subscribtion and I didnt want to pay more for “my intelligence”.\nAnd this bugs me a little bit that now in order to stand out as a “developer” you need to pay this and that susbcription to have access to “intelligence” and now you need to learn how to use these tools and let’s accept it being a little bit nostalgic about the feeling of how programming felt before…, but a lot of us dont even have a job to keep up with our basic bills but if you are not “AI fluent” and pay yours subscriptions on time you’re screwed and left behind the industry. So people to have access to this “high paying” jobs are now just the ones who can pay and “invest in their careers” paying for intelligence, agents and the next essential tool.</p>\n<h3>The API</h3>\n<p>The Remix action at /api/chat receives the message and full conversation history, then calls the OpenAI API:</p>\n<pre><code class=\"language-js\">// app/routes/api.chat.tsx\nexport const action: ActionFunction = async ({ request }) =&gt; {\n    const body = await request.json();\n    const { message, conversationHistory = [] } = body;\n    const reply = await getChatResponse(message, conversationHistory);\n    return json({ reply });\n};\n\n</code></pre>\n<pre><code class=\"language-js\">// app/utils/openai.server.ts\nexport async function getChatResponse(message, conversationHistory) {\n    const res = await fetch(&quot;https://api.openai.com/v1/chat/completions&quot;, {\n        method: &quot;POST&quot;,\n        headers: { &quot;Authorization&quot;: `Bearer ${process.env.OPENAI_API_KEY}` },\n        body: JSON.stringify({\n            model: &quot;gpt-4o-mini&quot;,\n            max_tokens: 80,\n            temperature: 0.7,\n            messages: [\n                { role: &quot;system&quot;, content: SYSTEM_PROMPT },\n                ...conversationHistory,\n                { role: &quot;user&quot;, content: message },\n            ],\n        }),\n    });\n    const data = await res.json();\n    return data.choices[0].message.content;\n}\n</code></pre>\n<h1>What’s Next</h1>\n<p>The natural next step is RAG — Retrieval-Augmented Generation. Right now, every request includes the full resume in the prompt. As I write more blog posts, I’ll want to bring that content in too, but injecting every post into every prompt gets expensive fast.</p>\n<p>The plan: chunk the content (resume sections + blog posts), generate embeddings at build time, store them in a JSON file, and at query time find only the most relevant chunks to inject. The chat gets richer context; the prompt stays lean.</p>\n<p>For now though, the card talks back. That’s enough.</p>\n","date":"2026-03-27"},"mandarin-vocab":{"slug":"mandarin-vocab","title":"普通话 — Building a Mandarin Vocabulary Catalog for My Daughter","body":"<h1>普通话 — Building a Mandarin Vocabulary Catalog for My Daughter</h1>\n<h2>The Why</h2>\n<p>In my first few months postpartum, I felt that familiar feeling many of us have: that we’re not doing much more than breastfeeding and caring for the baby. Being a restless person, I decided to start an experiment: speaking to my daughter in Mandarin, a language I’ve been studying for over 10 years.</p>\n<p>I began watching videos on how to foster bilingualism in babies at <a href=\"https://www.youtube.com/@mssiugi/videos\">mssiugi</a> and started writing a Google Doc to help me remember useful phrases. Things like <em>“you have mud on your knees”</em>, <em>“don’t pick your nose”</em>, <em>“let’s water the flowers”</em>. Real, situational language for a toddler’s world. Phrases I could actually use at the playground, at bedtime, during a meltdown. </p>\n<p>The doc worked as a collection tool but was terrible for reference while teaching. I couldn’t search it mid-conversation, it had no structure that matched how I actually use the vocabulary, and I had a growing list of phrases I needed to look up and hadn’t researched yet.</p>\n<p>So I built an app.</p>\n<h2>Why It Lives in My Portfolio</h2>\n<p>I could have built this as a private, offline tool. A notes app would have done the job technically. But there’s something meaningful about building real, functional software for a real personal need — and putting it on display.</p>\n<p>A todo app demonstrates CRUD. This demonstrates the same fundamentals but with a story attached: a parent learning a language to be more present with her child. </p>\n<p>It also showcases decisions I’m proud of: a research queue that promotes entries to the catalog, an accordion UI to handle 36 categories without overwhelming the user, a PWA manifest so it works from my phone’s home screen during an actual lesson. These are product decisions, not just technical ones.</p>\n<h2>The Architecture</h2>\n<h3>Data storage: Turso (libsql)</h3>\n<p>The app is deployed on Vercel, which means no persistent filesystem. Local SQLite would reset on every deploy. The options were a hosted Postgres (Neon, Railway) or a hosted SQLite-compatible database.</p>\n<p>I chose <strong>Turso</strong> — it’s a distributed SQLite platform, free tier is generous, and the schema needed zero changes from the local SQLite I was already using during development. The whole migration was a connection string swap.</p>\n<p>One thing worth documenting: the Prisma adapter for libsql (<code>@prisma/adapter-libsql</code>) was unreliable in this serverless context. After several failed deployments with HTTP 400 errors from the adapter layer, I bypassed Prisma entirely for the vocab queries and went directly to <code>@libsql/client</code> with raw SQL. The rest of the app (blog, etc.) still uses Prisma — just not these routes. Sometimes the pragmatic move is the right move.</p>\n<h3>Schema</h3>\n<p>Three models:</p>\n<p><strong>VocabCategory</strong> — a named group with an emoji. <em>Greetings, Animals, Bedtime, Emotions</em>.</p>\n<p><strong>VocabEntry</strong> — the actual vocabulary. Hanzi, pinyin, Spanish translation, optional notes, and a foreign key to the category.</p>\n<p><strong>VocabQueue</strong> — phrases I need to research. Just a description and optional context (<em>“bedtime”</em>, <em>“at the park”</em>). When I look one up, I “complete” it: fill in the hanzi/pinyin/translation, assign a category, and it moves to the catalog. The queue item is deleted.</p>\n<h3>Importing from the txt file</h3>\n<p>I had 161 entries spread across a messy document with inconsistent formatting. Rather than entering them manually, I wrote a one-time parser (<code>prisma/seed-mandarin.ts</code>) that reads the file, detects category headers versus entry blocks by analyzing Chinese character ranges, pinyin tone marks, and text length heuristics, and inserts everything into the database.</p>\n<p>It’s not perfect — some categories parsed ambiguously and needed cleanup in the UI — but it got me from zero to 161 entries in seconds instead of hours.</p>\n<h3>The PWA</h3>\n<p>Adding <code>manifest.json</code> and a handful of <code>&lt;meta&gt;</code> tags is all it takes to make a web app installable on a phone’s home screen. The app now shows up in my dock next to my other apps, opens without a browser chrome, and has the portfolio’s mauve pink as the status bar color. When I’m at the playground and my daughter points at something, I can open it in one tap.</p>\n<h2>UI Decisions</h2>\n<p><strong>Accordion categories.</strong> With 36 categories and 161 entries the naive list is unscrollable. Categories start collapsed. Click to expand. Search auto-expands matching categories and filters in real time — hanzi, pinyin, and English all searchable from one input.</p>\n<p><strong>Click to edit.</strong> No separate edit page. Click a category name or an entry row and it expands into an inline form. Save or cancel. The URL tracks state via query params so nothing gets lost on a page transition.</p>\n<p><strong>The research queue.</strong> This is the feature I’m most happy with conceptually. When I’m mid-lesson and think <em>“how do I say ‘be careful, that’s sharp’?”</em> I jot it into the queue with a tap. Later I research it, fill in the translation, and complete it. It moves to the catalog. The friction of not knowing something in the moment doesn’t interrupt the lesson.</p>\n<p><strong>Reusable components.</strong> <code>PillButton</code>, <code>VocabInput</code>, <code>InlineFormCard</code> — small atoms extracted from the repetitive patterns across the catalog and queue views. Both routes look consistent without duplicating styles.</p>\n<h2>Next Steps</h2>\n<p><strong>Audio.</strong> The most important thing missing. Reading pinyin and characters is useful for me, but hearing the correct pronunciation is what actually sticks — for her and for me. I want to add the ability to record myself saying each phrase. My voice, not a text-to-speech API, because she needs to recognize <em>how I say it</em>, not how a computer does.</p>\n<p><strong>Flashcard mode.</strong> A simple study interface — show the hanzi, ask for the translation, flip to reveal. Spaced repetition would be ideal eventually, but even a random shuffle through a category would be useful for drilling before a lesson.</p>\n<p><strong>Print view.</strong> A clean printable layout per category for offline reference or for grandparents who want to learn alongside her.</p>\n<p><strong>Better category management.</strong> Right now categories can’t be merged or reordered. Some parsed ambiguously from the import (hello, “twist” with 23 entries). A drag-to-reorder and a merge-categories action would clean that up.</p>\n<hr />\n<p>The app is live at <a href=\"https://tatimoreira.me/mandarin\">tatimoreira.me/mandarin</a>. The code is the same codebase as this portfolio — same Remix app, same Vercel deployment, one more route.</p>\n<p>She’s almost three now. She speaks more Mandarin than I do now. We’re doing alright.</p>\n","date":"2026-04-14"}};
